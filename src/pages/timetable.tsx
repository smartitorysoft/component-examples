import React from 'react';
import TimetableView from 'views/Timetable/Timetable';
import type { NextPage } from 'next';

const Timetable: NextPage = () => <TimetableView />;

export default Timetable;
